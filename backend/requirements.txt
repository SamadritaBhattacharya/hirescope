# === Core Framework ===
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
httpx==0.27.0
sse-starlette==2.1.0

# === LangChain / LangGraph ===
langgraph==0.2.14
langchain==0.2.14
langchain-core==0.2.32
langchain-groq==0.1.9
langchain-community==0.2.12

# === Vector Store & Embeddings ===
chromadb==0.5.5
sentence-transformers==3.0.1

# Pin the OpenTelemetry family together (same release train) so pip's
# resolver doesn't backtrack through every version trying to satisfy
# chromadb's loose transitive constraints — this is what was causing
# the long "Collecting opentelemetry_sdk-1.4x.0... 1.39.0... 1.38.0..."
# loop in the build log.
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp-proto-common==1.27.0
opentelemetry-exporter-otlp-proto-grpc==1.27.0
opentelemetry-proto==1.27.0

# onnxruntime (a chromadb dependency) requires this floor — pin it too,
# otherwise pip backtracks across protobuf versions as well.
protobuf>=4.25.8,<5

# === LLM APIs ===
groq==0.9.0

# === Search ===
tavily-python==0.3.3

# === GitHub ===
PyGithub==2.3.0

# === Parsers ===
pdfplumber==0.11.1
python-docx==1.1.2

# === LinkedIn Scraper ===
linkedin-scraper==2.11.2
selenium==4.23.0
webdriver-manager==4.0.2

# === PDF Export ===
weasyprint==62.3
jinja2==3.1.4

# === Data & Validation ===
pydantic==2.8.2
pydantic-settings==2.4.0
python-dotenv==1.0.1

# === Utilities ===
asyncio-throttle==1.0.2
tenacity==8.5.0
structlog==24.4.0
numpy==1.26.4